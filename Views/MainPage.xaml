<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Stargazer.Database.Entities"
             xmlns:viewmodels="clr-namespace:Stargazer.ViewModels"
             x:Class="Stargazer.MainPage"
             x:DataType="viewmodels:StarsViewModel">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Entry 
                x:Name="Name"
                Placeholder="Enter Star Name"
                TextChanged="OnNameTextChanged" />

            <Button
                x:Name="SaveBtn"
                Text="Save" 
                SemanticProperties.Hint="Saves Star to database"
                Clicked="OnSaveClicked"
                HorizontalOptions="Fill" />

            <ListView ItemsSource="{Binding Stars}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Star">
                        <ViewCell>
                            <Grid Padding="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Column="1"
                                   Text="{Binding Id}"
                                   FontAttributes="Italic" />
                                <Label Grid.Row="1"
                                   Grid.Column="1"
                                   Text="{Binding Name}"
                                   FontAttributes="Bold"
                                   VerticalOptions="End" />
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
